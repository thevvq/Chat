extends ../layouts/default.pug

block main
    .chat-container
        // Cột trái - Danh sách bạn bè / nhóm
        .sidebar
            ul.chat-list
                li.active 
                    i.fa-solid.fa-user
                    span Tin nhắn
                li 
                    i.fa-solid.fa-user
                    span Liên hệ
                li 
                    i.fa-solid.fa-user
                    span Cài đặt

        // Cột giữa - Danh sách tin nhắn gần đây
        .conversation-list
            h4 Cuộc trò chuyện gần đây
            .conversation
                strong Em Yêu:
                span Anh ơi, sáng qua sao chưa rep em 😢
            .conversation
                strong Minh Phú:
                span Gửi file chưa ông?

        // Cột phải - Khu vực chat chính
        .chat-window
            .inner-body

                for chat in chats
                    div(class=(chat.user_id == user.id ? 'inner-outgoing' : 'inner-incoming'))
                        if chat.user_id != user.id
                            .inner-name #{chat.infoUser.fullName} 
                        .inner-content #{chat.content}
                //- .inner-outgoing
                //-     .inner-content Thế à? Oke em yêu.
                //- .inner-incoming
                //-     .inner-name Em Yêu 
                //-     .inner-content Anh đến chưa anh?
                //- .inner-outgoing
                //-     .inner-content Anh đến rồi, nhưng đâu thấy ai ở nhà đâu em?
                //- .inner-incoming
                //-     .inner-name Em Yêu 
                //-     .inner-content Thì em bảo là không ai ở nhà mà :))
            .inner-foot
                form(class='inner-form' action='')
                    input(type="text" placeholder='Nhập nội dung...' name='content' autocomplete="off")
                    button
                        i.fa-solid.fa-paper-plane
