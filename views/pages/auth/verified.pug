extends ../../layouts/auth.pug

block main
  .auth-wrapper
    .auth-card.auth-success
      .auth-icon(role='img' aria-hidden='true') ✅
      h1 Xác thực thành công
      p.auth-subtitle Tài khoản của bạn đã được xác thực. Bạn có thể đăng nhập ngay bây giờ.
      .auth-actions.mt-4
        a.btn.btn-login(href='/auth/login') Đến trang đăng nhập
