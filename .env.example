# Mail (SMTP) configuration for sending verification emails
# Copy to `.env` and fill with your credentials. Do NOT commit `.env` to git.

# Use Gmail SMTP with App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=ujca jktn zqke ytje
EMAIL_FROM=Your Name <your-email@gmail.com>

# Optional: app port
PORT=3000
